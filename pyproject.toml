[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-assist-context-service"
version = "0.1.0"
description = "Stdlib-only Python package for AI assist context-domain behavior."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
