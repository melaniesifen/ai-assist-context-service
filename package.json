{
  "name": "ai-assist-context-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Domain-layer context validation, consent, normalization, and provenance helpers for AI Assist.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
